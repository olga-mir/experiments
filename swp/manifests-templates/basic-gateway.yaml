name: projects/$GCP_PROJECT_ID/locations/$GCP_REGION/gateways/${experiment_name}-swp
type: SECURE_WEB_GATEWAY
addresses: ["10.128.0.99"]
ports: [443]
certificateUrls: ["projects/$GCP_PROJECT_ID/locations/$GCP_REGION/certificates/${experiment_name}-cert"]
gatewaySecurityPolicy: projects/$GCP_PROJECT_ID/locations/$GCP_REGION/gatewaySecurityPolicies/${experiment_name}-policy
network: projects/$GCP_PROJECT_ID/global/networks/$vpc_name
subnetwork: projects/$GCP_PROJECT_ID/regions/$GCP_REGION/subnetworks/$subnet_name
scope: samplescope
