version: '3'

vars:
  PROJECT_ID:
    sh: gcloud config get-value project 2>/dev/null || echo $GOOGLE_CLOUD_PROJECT
  ENGINE_ID: "4092540608256671744"
  ENGINE_LOCATION: "us-east1"
  ENGINE_BASE_URL: "https://{{.ENGINE_LOCATION}}-aiplatform.googleapis.com/v1beta1/projects/{{.PROJECT_ID}}/locations/{{.ENGINE_LOCATION}}/reasoningEngines/{{.ENGINE_ID}}"

tasks:
  preflight:
    desc: Check required environment variables
    cmds:
      - |
        ERRORS=0
        if [ -z "{{.PROJECT_ID}}" ]; then
          echo "❌ GOOGLE_CLOUD_PROJECT is not set and gcloud is not configured"
          ERRORS=$((ERRORS+1))
        fi
        if [ -z "$SCREENSHOTS_BUCKET_NAME" ]; then
          echo "⚠️  SCREENSHOTS_BUCKET_NAME is not set (screenshots will fail)"
        fi
        if [ -z "$SIMULATION_BASE_URL" ]; then
          echo "⚠️  SIMULATION_BASE_URL is not set (backend simulation calls will fail)"
        fi
        if [ "$ERRORS" -gt 0 ]; then
          exit 1
        fi
        echo "✅ Preflight checks passed (Project: {{.PROJECT_ID}})"

  install:
    desc: Install dependencies
    cmds:
      - agents-cli install --clean

  run:
    desc: Run the ambient agent (one-off prompt)
    deps: [preflight]
    cmds:
      - agents-cli run "{{.CLI_ARGS | default "Perform a sweep of the conference"}}"

  playground:
    desc: Open the ambient agent playground
    deps: [preflight]
    cmds:
      - agents-cli playground

  server:
    desc: Run the FastAPI server locally (enables triggers)
    deps: [preflight]
    cmds:
      - uv run python app/fast_api_app.py

  trigger:
    desc: Locally trigger the ambient agent via Webhook (simulates Cron)
    cmds:
      - |
        curl -X POST http://localhost:8000/apps/ambient_ai_attendee/trigger/webhook \
          -H "Content-Type: application/json" \
          -d '{"message": "tick"}'

  logs:
    desc: Tail recent Reasoning Engine logs (agent stdout/stderr)
    deps: [preflight]
    cmds:
      - |
        gcloud logging read \
          'resource.type="aiplatform.googleapis.com/ReasoningEngine" AND resource.labels.reasoning_engine_id="{{.ENGINE_ID}}"' \
          --project={{.PROJECT_ID}} \
          --limit={{.CLI_ARGS | default "50"}} \
          --order=asc \
          --format="table(timestamp,textPayload)"

  session:create:
    desc: Create a persistent session for the ambient agent and save ID to .ambient_session_id
    deps: [preflight]
    cmds:
      - |
        TOKEN=$(gcloud auth print-access-token)
        RESPONSE=$(curl -s -X POST "{{.ENGINE_BASE_URL}}/sessions" \
          -H "Authorization: Bearer $TOKEN" \
          -H "Content-Type: application/json" \
          -d '{"user_id": "scheduler"}')
        # Response is an LRO; session name is in response['name']
        SESSION_ID=$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('response', {}).get('name', '').split('/')[-1])" 2>/dev/null)
        if [ -z "$SESSION_ID" ]; then
          # Fallback for synchronous response (v1beta1 can sometimes be sync depending on region/load)
          SESSION_ID=$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('name','').split('/')[-1])" 2>/dev/null)
        fi
        if [ -z "$SESSION_ID" ]; then
          echo "❌ Failed to create session"
          echo "$RESPONSE"
          exit 1
        fi
        echo "$SESSION_ID" > .ambient_session_id
        echo "✅ Session created: $SESSION_ID (saved to .ambient_session_id)"

  session:list:
    desc: List all sessions for the ambient agent
    deps: [preflight]
    cmds:
      - |
        TOKEN=$(gcloud auth print-access-token)
        curl -s "{{.ENGINE_BASE_URL}}/sessions?filter=user_id%3Dscheduler" \
          -H "Authorization: Bearer $TOKEN" | \
          python3 -c "
        import json, sys
        data = json.load(sys.stdin)
        sessions = data.get('sessions', [])
        if not sessions:
            print('No sessions found')
        else:
            for s in sessions:
                print(f\"{s['name'].split('/')[-1]}  updateTime={s.get('updateTime','')}  turns={len(s.get('events',[]))}\")
        "

  session:reset:
    desc: Delete the current session and create a fresh one (use between test runs)
    deps: [preflight]
    cmds:
      - |
        if [ -f .ambient_session_id ]; then
          SESSION_ID=$(cat .ambient_session_id)
          echo "Deleting session $SESSION_ID..."
          TOKEN=$(gcloud auth print-access-token)
          curl -s -X DELETE "{{.ENGINE_BASE_URL}}/sessions/$SESSION_ID" \
            -H "Authorization: Bearer $TOKEN"
          rm .ambient_session_id
          echo "✅ Session deleted"
        else
          echo "No .ambient_session_id found, skipping delete"
        fi
      - task: session:create

  deploy:
    desc: Deploy the ambient agent to GCP Agent Platform
    deps: [preflight]
    cmds:
      - |
        if [ -z "$SIMULATION_BASE_URL" ]; then
          echo "❌ SIMULATION_BASE_URL is not set"
          exit 1
        fi
        agents-cli deploy --project {{.PROJECT_ID}} --update-env-vars SIMULATION_BASE_URL=$SIMULATION_BASE_URL

  setup:infra:
    desc: Enable APIs, create Service Account, and grant IAM roles for Cloud Scheduler
    deps: [preflight]
    vars:
      SA_NAME: "ambient-attendee-scheduler"
      SA_EMAIL: "{{.SA_NAME}}@{{.PROJECT_ID}}.iam.gserviceaccount.com"
      CLOUD_RUN_SERVICE: "ai-attendee-ambient"
      CLOUD_RUN_REGION: "{{.CLOUD_RUN_REGION | default \"us-east1\"}}"
    cmds:
      - |
        echo "Enabling required GCP APIs..."
        gcloud services enable cloudscheduler.googleapis.com --project={{.PROJECT_ID}}
        gcloud services enable run.googleapis.com --project={{.PROJECT_ID}}
        gcloud services enable aiplatform.googleapis.com --project={{.PROJECT_ID}}
      - |
        if ! gcloud iam service-accounts describe {{.SA_EMAIL}} >/dev/null 2>&1; then
          echo "Creating service account {{.SA_EMAIL}}..."
          gcloud iam service-accounts create {{.SA_NAME}} --display-name="Ambient Attendee Scheduler"
        fi
        echo "Granting Reasoning Engine User role to {{.SA_EMAIL}}..."
        gcloud projects add-iam-policy-binding {{.PROJECT_ID}} \
          --member="serviceAccount:{{.SA_EMAIL}}" \
          --role="roles/aiplatform.user"
      - |
        echo "Granting Cloud Run Invoker role to {{.SA_EMAIL}} on service {{.CLOUD_RUN_SERVICE}}..."
        gcloud run services add-iam-policy-binding {{.CLOUD_RUN_SERVICE}} \
          --member="serviceAccount:{{.SA_EMAIL}}" \
          --role="roles/run.invoker" \
          --region="{{.CLOUD_RUN_REGION}}" \
          --project={{.PROJECT_ID}}

  job:run:
    desc: Manually trigger the Cloud Scheduler job
    deps: [preflight]
    cmds:
      - gcloud scheduler jobs run ambient-cron --location="{{.GOOGLE_CLOUD_LOCATION | default "us-central1"}}"

  logs:raw:
    desc: Tail most recent Reasoning Engine logs in reverse order (newest first)
    deps: [preflight]
    cmds:
      - |
        gcloud logging read \
          'resource.type="aiplatform.googleapis.com/ReasoningEngine" AND resource.labels.reasoning_engine_id="{{.ENGINE_ID}}"' \
          --project={{.PROJECT_ID}} \
          --limit={{.CLI_ARGS | default "20"}} \
          --order=desc \
          --format="table(timestamp,textPayload)"

  schedule:
    desc: Schedule the ambient agent to run periodically using Cloud Scheduler
    deps: [preflight]
    vars:
      SA_EMAIL: "ambient-attendee-scheduler@{{.PROJECT_ID}}.iam.gserviceaccount.com"
      SCHEDULER_LOCATION: "{{.GOOGLE_CLOUD_LOCATION | default \"us-central1\"}}"
    cmds:
      - |
        if [ -z "$AGENT_URL" ]; then
          echo "❌ AGENT_URL environment variable is not set."
          echo "   For Reasoning Engine: AGENT_URL=https://us-east1-aiplatform.googleapis.com/v1/projects/<NUM>/locations/us-east1/reasoningEngines/<ID>"
          echo "   For Cloud Run:        AGENT_URL=https://<service>.run.app/apps/ambient_ai_attendee/trigger/webhook"
          exit 1
        fi

        # Pick up session ID from env var or saved file
        if [ -n "$AMBIENT_SESSION_ID" ]; then
          SESSION_ID="$AMBIENT_SESSION_ID"
        elif [ -f .ambient_session_id ]; then
          SESSION_ID=$(cat .ambient_session_id)
        fi

        # Build request body
        BODY_FILE=$(mktemp)
        if [ -n "$SESSION_ID" ]; then
          echo "{\"input\": {\"message\": \"tick\", \"user_id\": \"scheduler\", \"session_id\": \"$SESSION_ID\"}}" > "$BODY_FILE"
          echo "Using session: $SESSION_ID"
        else
          echo '{"input": {"message": "tick", "user_id": "scheduler"}}' > "$BODY_FILE"
          echo "⚠️  No session ID set — each run will create a new session (no continuity). Run: task session:create"
        fi

        # Cloud Scheduler OAuth requires .googleapis.com; use OIDC for Cloud Run (.run.app)
        if echo "$AGENT_URL" | grep -q '\.googleapis\.com'; then
          TARGET_URI="${AGENT_URL}:streamQuery"
          AUTH_FLAGS="--oauth-service-account-email={{.SA_EMAIL}} --oauth-token-scope=https://www.googleapis.com/auth/cloud-platform"
          echo "Using OAuth auth (googleapis.com endpoint)"
        else
          TARGET_URI="${AGENT_URL}"
          BASE_URL=$(echo "$AGENT_URL" | sed 's|\(https://[^/]*\).*|\1|')
          AUTH_FLAGS="--oidc-service-account-email={{.SA_EMAIL}} --oidc-token-audience=${BASE_URL}"
          echo "Using OIDC auth (Cloud Run endpoint, audience: ${BASE_URL})"
        fi

        # Check if job exists, if so update, otherwise create
        if gcloud scheduler jobs describe ambient-cron --location="{{.SCHEDULER_LOCATION}}" >/dev/null 2>&1; then
          echo "Updating existing scheduler job..."
          gcloud scheduler jobs update http ambient-cron \
            --schedule="*/1 * * * *" \
            --uri="${TARGET_URI}" \
            --http-method=POST \
            --update-headers="Content-Type=application/json" \
            --message-body-from-file="$BODY_FILE" \
            ${AUTH_FLAGS} \
            --location="{{.SCHEDULER_LOCATION}}"
        else
          echo "Creating new scheduler job..."
          gcloud scheduler jobs create http ambient-cron \
            --schedule="*/1 * * * *" \
            --uri="${TARGET_URI}" \
            --http-method=POST \
            --headers="Content-Type=application/json" \
            --message-body-from-file="$BODY_FILE" \
            ${AUTH_FLAGS} \
            --location="{{.SCHEDULER_LOCATION}}"
        fi
        rm "$BODY_FILE"
