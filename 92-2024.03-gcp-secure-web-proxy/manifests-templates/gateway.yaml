name: projects/$GCP_PROJECT_ID/locations/$GCP_REGION/gateways/swp
type: SECURE_WEB_GATEWAY
addresses: ["10.0.0.9"]
ports: [443]
certificateUrls: ["projects/$GCP_PROJECT_ID/locations/$GCP_REGION/certificates/swp-cert"]
gatewaySecurityPolicy: projects/$GCP_PROJECT_ID/locations/$GCP_REGION/gatewaySecurityPolicies/$policy_name
network: projects/$GCP_PROJECT_ID/global/networks/$vpc_name
subnetwork: projects/$GCP_PROJECT_ID/regions/$GCP_REGION/subnetworks/$subnet_name
scope: samplescope
